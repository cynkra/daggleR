% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{list_schedules}
\alias{list_schedules}
\title{List schedules for a DAG}
\usage{
list_schedules(name, base_url = NULL)
}
\arguments{
\item{name}{Character string. Name of the DAG.}

\item{base_url}{Base URL of the daggle API. See Details.}
}
\value{
A data.frame with columns \code{id}, \code{cron}, \code{source}, \code{enabled},
\code{next_run}. The \code{source} column is \code{"yaml"} for schedules declared in
the DAG's YAML and \code{"runtime"} for ones added via the API. \code{next_run}
is an RFC3339 timestamp or the empty string when the schedule is
disabled. A \code{params} list-column is present when any schedule carries
parameter overrides.
}
\description{
Returns every schedule registered for the DAG — both YAML-defined schedules
and ones added at runtime via \code{\link[=add_schedule]{add_schedule()}}.
}
