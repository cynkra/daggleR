% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{archive_run}
\alias{archive_run}
\title{Download a tamper-evident run archive}
\usage{
archive_run(
  name,
  run_id,
  dest = tempfile(fileext = ".tar.gz"),
  base_url = NULL
)
}
\arguments{
\item{name}{Character string. Name of the DAG.}

\item{run_id}{Character string. Run ID, or \code{"latest"} for the most recent.}

\item{dest}{Character string. Local file path to write the archive to.
Defaults to a temporary file with extension \code{.tar.gz}.}

\item{base_url}{Base URL of the daggle API. See Details.}
}
\value{
The absolute path to \code{dest}, returned invisibly.
}
\description{
Creates (or refreshes) the server-side archive and writes the \code{.tar.gz}
bytes to \code{dest}. Use \code{\link[=verify_archive]{verify_archive()}} afterwards to confirm integrity.
}
\details{
This is the first wrapper in daggleR that returns a binary response.
Future binary downloads should mirror this shape: resolve the base URL
with \code{\link[=resolve_base_url]{resolve_base_url()}}, build the request directly via
\code{\link[httr2:request]{httr2::request()}} and \code{\link[httr2:req_url]{httr2::req_url_path_append()}} (skipping
\code{daggle_request()}, which always parses the response as JSON), then
persist with \code{\link[=writeBin]{writeBin()}} on \code{\link[httr2:resp_body_raw]{httr2::resp_body_raw()}}.
}
