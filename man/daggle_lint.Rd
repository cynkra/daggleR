% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{daggle_lint}
\alias{daggle_lint}
\title{Run \verb{daggle lint} on a DAG}
\usage{
daggle_lint(dag, check_packages = FALSE, daggle_bin = "daggle")
}
\arguments{
\item{dag}{Character string. DAG name or path to a DAG YAML file.}

\item{check_packages}{Logical. If \code{TRUE}, also verify that R packages
required by R-based step types (\code{rmd}, \code{coverage}, \code{pkgdown}, \code{pins}, ...)
are installed. Slower; runs a single \code{Rscript} invocation. Default \code{FALSE}.}

\item{daggle_bin}{Character string. Path to the \code{daggle} binary. Defaults
to \code{"daggle"} (resolved via PATH).}
}
\value{
A data.frame with columns \code{path}, \code{line}, \code{col}, \code{severity}
(\code{"error"}, \code{"warning"}, \code{"info"}), \code{code}, and \code{message}. Returns a
zero-row data.frame when the DAG passes cleanly.
}
\description{
Shells out to the local \code{daggle} CLI to run semantic diagnostics on a DAG
(missing scripts, unresolvable secrets, unknown notification channels, and
optionally missing R packages). Returns a tidy data.frame of diagnostics.
}
\details{
Requires the \code{daggle} binary on the user's PATH.
}
\examples{
\dontrun{
diagnostics <- daggle_lint("etl-pipeline")
stopifnot(nrow(diagnostics[diagnostics$severity == "error", ]) == 0)
}
}
