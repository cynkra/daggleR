% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{daggle_archive_info}
\alias{daggle_archive_info}
\title{Create and describe a tamper-evident run archive}
\usage{
daggle_archive_info(name, run_id, base_url = NULL)
}
\arguments{
\item{name}{Character string. Name of the DAG.}

\item{run_id}{Character string. Run ID, or \code{"latest"} for the most recent.}

\item{base_url}{Base URL of the daggle API. See Details.}
}
\value{
A list with elements \code{path} (absolute path on the server),
\code{files} (count), \code{bytes} (total size), \code{created_at} (RFC3339 UTC).
}
\description{
Triggers creation (or refresh) of the tamper-evident \code{.tar.gz} archive
for a run on the server and returns the archive metadata without
downloading the bytes. The archive is stored server-side at
\verb{DAGGLE_DATA_DIR/archives/\{name\}_\{run_id\}.tar.gz}; use \code{\link[=daggle_archive_run]{daggle_archive_run()}}
to stream it locally.
}
