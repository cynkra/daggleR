% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init.R
\name{daggle_init_dag}
\alias{daggle_init_dag}
\title{Scaffold a new DAG YAML under \verb{.daggle/}}
\usage{
daggle_init_dag(
  name,
  template = c("minimal", "data-pipeline", "pkg-check", "pkg-release"),
  dir = getwd(),
  overwrite = FALSE
)
}
\arguments{
\item{name}{Character string. Name for the DAG. Becomes the YAML file's
basename (\verb{.daggle/<name>.yaml}) and the value of the \verb{name:} field in
the generated YAML. Must not be empty and must not contain \code{/} or \verb{\\}.}

\item{template}{Template to scaffold. One of \code{"minimal"},
\code{"data-pipeline"}, \code{"pkg-check"}, or \code{"pkg-release"}.}

\item{dir}{Character string. Directory to create \verb{.daggle/} inside.
Defaults to the current working directory.}

\item{overwrite}{Logical. If \code{FALSE} (default), errors when the target
file already exists. Set to \code{TRUE} to replace it.}
}
\value{
The absolute path to the written YAML file, returned invisibly.
}
\description{
Creates \verb{<dir>/.daggle/} if missing and writes \verb{<dir>/.daggle/<name>.yaml}
from a built-in template. Useful for bootstrapping a new DAG from an R
session without leaving the IDE. This mirrors the \verb{daggle init <template>}
CLI command.
}
\seealso{
\code{\link[=daggle_register_project]{daggle_register_project()}} to register the project with the daggle
daemon so newly scaffolded DAGs become visible over the HTTP API.
}
