% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{daggle_request}
\alias{daggle_request}
\title{Send a request to the daggle API}
\usage{
daggle_request(
  path,
  method = c("GET", "POST", "DELETE"),
  query = NULL,
  body = NULL,
  simplify = FALSE,
  base_url = NULL
)
}
\arguments{
\item{path}{Character vector of URL path segments appended after the
base URL via \code{\link[httr2:req_url]{httr2::req_url_path_append()}}. Example:
\code{c("api", "v1", "dags", name, "runs", run_id)}.}

\item{method}{HTTP method. One of \code{"GET"}, \code{"POST"}, \code{"DELETE"}.}

\item{query}{Named list of query parameters, or \code{NULL}. Elements whose
value is \code{NULL} are dropped (so callers can pass optional filters
without conditional wiring).}

\item{body}{Named list serialised as a JSON request body, or \code{NULL}.}

\item{simplify}{Passed to \code{\link[httr2:resp_body_raw]{httr2::resp_body_json()}} as \code{simplifyVector}.
Set \code{TRUE} for collection endpoints that should return a data.frame;
leave \code{FALSE} for single-object endpoints that should return a list.}

\item{base_url}{Character string or \code{NULL}. Passed to \code{\link[=resolve_base_url]{resolve_base_url()}}.}
}
\value{
Parsed JSON response — a list, or a data.frame when
\code{simplify = TRUE}.
}
\description{
Internal helper used by the API wrappers. Builds an \code{httr2} request
against the resolved base URL, performs it, and parses the JSON body.
Centralises the boilerplate so each wrapper only declares the path,
method, and optional query/body.
}
\keyword{internal}
