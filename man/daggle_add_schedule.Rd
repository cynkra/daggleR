% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{daggle_add_schedule}
\alias{daggle_add_schedule}
\title{Add a runtime schedule to a DAG}
\usage{
daggle_add_schedule(name, cron, params = NULL, enabled = TRUE, base_url = NULL)
}
\arguments{
\item{name}{Character string. Name of the DAG.}

\item{cron}{Character string. Cron expression (e.g. \code{"0 7 * * *"}).}

\item{params}{Named list of parameter overrides passed to each triggered
run, or \code{NULL}.}

\item{enabled}{Logical. Whether the schedule fires immediately. Defaults
to \code{TRUE}.}

\item{base_url}{Base URL of the daggle API. See Details.}
}
\value{
A list describing the created schedule: \code{id}, \code{cron}, \code{source},
\code{enabled}, \code{next_run}, and (when set) \code{params}.
}
\description{
Registers a new cron schedule without editing the DAG's YAML. The schedule
is stored server-side with \code{source = "runtime"} and can be removed with
\code{\link[=daggle_remove_schedule]{daggle_remove_schedule()}}.
}
