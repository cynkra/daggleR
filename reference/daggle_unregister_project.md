# Unregister a project

Unregister a project

## Usage

``` r
daggle_unregister_project(name, base_url = NULL)
```

## Arguments

- name:

  Character string. Name of the project to unregister.

- base_url:

  Base URL of the daggle API. See Details.

## Value

A list with element: `name`.
