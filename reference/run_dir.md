# Get the current run directory

Get the current run directory

## Usage

``` r
run_dir()
```

## Value

Character string from the `DAGGLE_RUN_DIR` environment variable.

## Examples

``` r
if (FALSE) { # \dontrun{
run_dir()
} # }
```
