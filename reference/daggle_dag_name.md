# Get the current DAG name

Get the current DAG name

## Usage

``` r
daggle_dag_name()
```

## Value

Character string from the `DAGGLE_DAG_NAME` environment variable.

## Examples

``` r
if (FALSE) { # \dontrun{
daggle_dag_name()
} # }
```
