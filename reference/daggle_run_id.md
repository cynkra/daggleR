# Get the current daggle run ID

Get the current daggle run ID

## Usage

``` r
daggle_run_id()
```

## Value

Character string from the `DAGGLE_RUN_ID` environment variable.

## Examples

``` r
if (FALSE) { # \dontrun{
daggle_run_id()
} # }
```
