# Check API health

Check API health

## Usage

``` r
daggle_health(base_url = NULL)
```

## Arguments

- base_url:

  Base URL of the daggle API. See Details.

## Value

A list with elements: `status`, `version`, `uptime_seconds`.
