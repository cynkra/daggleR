# List registered projects

List registered projects

## Usage

``` r
daggle_list_projects(base_url = NULL)
```

## Arguments

- base_url:

  Base URL of the daggle API. See Details.

## Value

A data.frame with columns: `name`, `path`, `status`, `dags`.
